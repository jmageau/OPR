= form_for @financial_institution do |f|
  - if @financial_institution.errors.any?
    #error_explanation
      %h2= "#{pluralize(@financial_institution.errors.count, "error")} prohibited this financial_institution from being saved:"
      %ul
        - @financial_institution.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
