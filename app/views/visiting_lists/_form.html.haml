= form_for @visiting_list do |f|
  - if @visiting_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@visiting_list.errors.count, "error")} prohibited this visiting_list from being saved:"
      %ul
        - @visiting_list.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :customer_id
    = f.number_field :customer_id
  .actions
    = f.submit 'Save'
